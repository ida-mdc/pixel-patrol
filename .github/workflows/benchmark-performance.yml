name: Run Benchmarks

on:
  workflow_dispatch: # Keep this for later
  push: # Remove later
    branches:
      - 'benchmark-performance'

jobs:
  benchmark:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # Install your package
          pip install uv
          uv venv
          uv pip install -e "packages/pixel-patrol/.[dev]"
          uv pip install pytest

      - name: Run Benchmarks
        # Generates benchmark_results1.csv
        run: |
          uv run python packages/pixel-patrol/tests/benchmarks/benchmark_e2e.py --mode quick --branch main

      - name: Generate Report
        # Generates REPORT.md, *.png, and writes tables to Job Summary
        run: |
              uv run python packages/pixel-patrol/tests/benchmarks/generate_benchmark_report.py \
                  --csv packages/pixel-patrol/tests/benchmarks/results/e2e_results.csv \
                  --figures

      - name: Upload Artifacts
        # Zips up the results so you can download them later
        # No commits are made to the repo!
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-report
          path: |
            packages/pixel-patrol/tests/benchmarks/results/REPORT.md
            packages/pixel-patrol/tests/benchmarks/results/assets/*
            packages/pixel-patrol/tests/benchmarks/results/*.csv
          retention-days: 30