[project]
name = "pixel-patrol"
version = "0.9.0"
description = "Image prevalidation tool - PixelPatrol and its add-ons packages"
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
authors = [
    { name = "Helmholtz Imaging Engineering & Support Unit MDC" }
]
requires-python = ">=3.11"

dependencies = [
    "pixel-patrol-base==0.9.0",
    "pixel-patrol-loader-bio==0.9.0",
    "pixel-patrol-geospatial==0.9.0",
]

[project.urls]
Homepage = "https://ida-mdc.github.io/pixel-patrol/"
Repository = "https://github.com/ida-mdc/pixel-patrol"

[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[tool.uv]
package = true

[dependency-groups]
dev = [
    "pytest>=8.3.5",
    "pytest-mock>=3.14.1",
]

[tool.uv.sources]
pixel-patrol-base = { path = "../pixel-patrol-base" }
pixel-patrol-loader-bio = { path = "../pixel-patrol-loader-bio" }
pixel-patrol-geospatial = { path = "../pixel-patrol-geospatial" }

[tool.setuptools.package-data]
"*" = ["readme_assets/*"]